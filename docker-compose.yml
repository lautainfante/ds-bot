services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ds-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./secrets:/opt/ds-bot/secrets:ro
